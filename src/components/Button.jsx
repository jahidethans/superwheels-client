

const Button = ({ styles }) => {
    return (
        
            <button type="button" className={`px-4 py-4 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none ${styles}`}>Get Started</button>
        
    );
};

export default Button;